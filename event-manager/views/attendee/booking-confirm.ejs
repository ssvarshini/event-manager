<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Booking Confirmation</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

  <div class="container py-5">
    <div class="card p-4">
        <!--Show confirmed booking and details-->
      <h1 class="h3 mb-3 text-primary">Booking Confirmed</h1>
      <p class="mb-2">Thank you, <strong><%= name %></strong>. Your tickets will be sent to your email.</p>
      <p class="mb-1"><strong>Event:</strong> <%= eventTitle %></p>
      <% if (standard > 0) { %>
        <!--Show standard tickets if booked-->
        <p class="mb-1"><strong>Standard Tickets:</strong> <%= standard %></p>
      <% } %>
      <% if (concession > 0) { %>
         <!--Show concession tickets if booked-->
        <p class="mb-4"><strong>Concession Tickets:</strong> <%= concession %></p>
      <% } %>
       <!--Button to go back to attendee home page-->
      <a href="/attendee" class="btn btn-primary">Back to Events</a>
    </div>
  </div>

</body>
</html>
