<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Organiser Login</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" />
  <link rel="stylesheet" href="/main.css" />
</head>
<body class="bg-light">

  <!--Page Banner with navigation-->
  <div class="banner text-center">
    <h1 class="mb-4">Organiser Login Page</h1>
    <!--Instruction for the login page-->
    <div class="siteInfo mb-4">
      <p> Enter password to access organiser portal</p>
    </div>  
    <a href="/" class="btn btn-primary1 mt-2 ms-2">Home Page</a>
  </div>

  <!--Login form-->
  <main class="container my-5">
    <div class="row justify-content-center">
      <div class="col-md-6 col-lg-6">
        <div class="card shadow">
          <div class="card-body">
            <% if (error) { %>
              <div class="alert alert-danger">
                <%= error %>
              </div>
            <% } %>

            <!--Login form heading-->
            <h2 class="h4 mb-4 text-start" style="color: var(--primary-color)">
              Secure Access Required
            </h2>

            <form method="POST" action="/organiser/login">
               <!--Password field in the login form-->
              <div class="mb-3 text-start">
                <label for="password" class="form-label">Password:</label>
                <input type="password" class="form-control" id="password" name="password" required>
              </div>
               <!--Button to submit the password-->
              <button type="submit" class="btn btn-primary d-flex align-items-center justify-content-center">
               Login
              </button>
              
            </form>

          </div>
        </div>
      </div>
    </div>
  </main>

</body>
</html>
